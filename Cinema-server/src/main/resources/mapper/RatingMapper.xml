<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinema.mapper.RatingMapper">

    <insert id="insert" parameterType="Rating" useGeneratedKeys="true" keyProperty="id">
        insert into rating (user_id, movie_id, rating, comment, create_time, update_time)
        values (#{userId}, #{movieId}, #{rating}, #{comment}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="Rating">
        update rating
        <set>
            <if test="rating != null">
                rating = #{rating},
            </if>
            <if test="comment != null">
                comment = #{comment},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from rating where id = #{id}
    </delete>

    <select id="getById" resultType="Rating">
        select * from rating where id = #{id}
    </select>

</mapper>
